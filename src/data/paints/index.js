// Paint data is loaded dynamically via import() in paintMatcher.js
// to keep the main bundle small. These re-exports are for direct access if needed.
export { SW_COLORS } from './sherwinWilliams';
export { BM_COLORS } from './benjaminMoore';
